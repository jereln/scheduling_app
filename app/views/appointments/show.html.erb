<p id="notice"><%= notice %></p>

<div>
  <strong>Date:</strong> <%= @appointment.date %>
</div>

<div>
  <strong>time</strong> <%= @appointment.start_time %> - <%= @appointment.end_time %>
</div>

<div>
 <strong>Therapist</strong> <%= @appointment.therapist.full_name %>
</div>

<div>
 <strong>Client</strong> <%= @appointment.client.full_name %>
</div>

<div>
 <strong>reserved?</strong> <%= @appointment.reserved %>
</div>


<% if @appointment.reserved %>
  <%= link_to 'Cancel Appointment', appointment_path(@appointment, appointment: {reserved: false}), method: :patch, data: {confirm: 'Are you sure?'} %>
<% else %>
  <%= button_to 'Reserve Appointment', appointment_path(@appointment, appointment: {reserved: true}), method: :patch, data: {confirm: 'Are you sure?'}%>
  |
<% end %>

<% if policy(@appointment).update? %>
  <%= link_to 'Edit', edit_appointment_path(@appointment) %>
  |
<% end %>

<%= link_to 'Back', root_path %>
