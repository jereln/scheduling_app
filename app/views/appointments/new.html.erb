<section class="hero">
  <%= render 'layouts/nav' %>
    <div class="hero-text">
      <h1>Create an available slot</h1><br>
      <h3>So that potential clients can make a reservation</h3>
    </div>
</section>

<%= render 'form' %>

<%= link_to 'Back', appointments_path %>
