<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Book a Massage" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to 'Home', root_path%></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if user_signed_in? %>
        <li class="signed-in-as">Signed in as <%= current_user.username %></li>
        <li><%= link_to 'Sign Out', sign_out_path %></li>
      <% else %>
        <li><%= link_to 'Sign In', sign_in_path %></li>
        <li><%= link_to 'Create An Account', sign_up_path %></li>
      <% end %>
        </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">
    <% if user_signed_in? %>
      <li><%= link_to 'Your Account', account_path %></li>
    <% end %>
    </ul>
  </section>
</nav>
<section class="hero">
</section>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>